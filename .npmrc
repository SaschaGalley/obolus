# Lower install concurrency to keep peak RAM down on small build hosts
# (e.g. Coolify VPS). Defaults are network-concurrency=16, child-concurrency=5,
# which combined with a parallel build of multiple workspace packages can
# OOM-kill the container mid-install.
network-concurrency=4
child-concurrency=2
